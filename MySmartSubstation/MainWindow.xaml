<Window x:Class="MySmartSubstation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MySmartSubstation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
       <StackPanel Margin="20">
    <TextBlock Text="HỆ THỐNG GIÁM SÁT TRẠM" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Tên trạm: " Margin="5"/>
        <TextBlock x:Name="txtStationName" Grid.Row="0" Grid.Column="1" Text="---" FontWeight="Bold" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Công suất: " Margin="5"/>
        <TextBlock x:Name="txtPower" Grid.Row="1" Grid.Column="1" Text="0 W" Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Trạng thái: " Margin="5"/>
        <TextBlock x:Name="txtStatus" Grid.Row="2" Grid.Column="1" Text="Đang chờ..." Margin="5"/>
    </Grid>
    <Label Content="ĐIỀU CHỈNH DÒNG ĐIỆN (A)" HorizontalAlignment="Center" Margin="0,10,0,0"/>
<Slider x:Name="sldCurrent" 
        Minimum="0" Maximum="10" 
        TickFrequency="0.1" IsSnapToTickEnabled="True" 
        ValueChanged="sldCurrent_ValueChanged" 
        Margin="20,10"/>
<TextBlock x:Name="txtCurrentDisplay" Text="0.0 A" FontSize="20" HorizontalAlignment="Center"/>
    <Button Content="ĐỌC DỮ LIỆU" Click="btnMeasure_Click" Margin="0,20,0,0" Height="40" 
        HorizontalAlignment="Center"
         Background="DarkBlue" Foreground="White"/>
</StackPanel>
    </Grid>
</Window>
